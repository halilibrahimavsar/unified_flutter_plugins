name: unified_flutter_features
description: A comprehensive collection of reusable Flutter components and features following Clean Architecture principles.
version: 1.0.0
homepage: https://github.com/your-username/unified_flutter_features

environment:
  sdk: '>=2.19.0 <4.0.0'
  flutter: ">=3.0.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.1.6
  connectivity_plus: ^6.1.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.1.0

flutter:
  uses-material-design: true