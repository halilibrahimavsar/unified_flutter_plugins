enum LoginLoadStatus {
  initial,
  loading,
  success,
  error,
}

enum AuthStatus {
  initial,
  loading,
  authenticated,
  failure,
  lockedOut,
}

enum SettingsStatus {
  initial,
  loading,
  success,
  error,
}
